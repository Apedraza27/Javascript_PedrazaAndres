class Rectangulo{
    constructor(ancho, alto){
        this.ancho = ancho;
        this.alto = alto;
    }
    displayDetails() {
        console.log(`ancho: ${this.ancho}`); 
      }
    displayDetails() {
        console.log(`alto: ${this.alto}`); 
      }
}